import { StyleSheet } from "react-native-web";

export const styles = StyleSheet.create({
    
    label: {
        fontSize: 18,
        color: "#fff",
        fontWeight: "bold",
        marginBotton: 8,
    },
    input: {
        fontSize: 18,
        color: "#fff",
        fontWeight: "bold",
        marginBotton: 8,
        backgroundColor: "#fff",
        borderRadius: 16,
        fontFamily: 'Roboto Slab'
    }
})